[package]
name = "rtc_server"
version = "0.1.0"
edition = "2021"

[dependencies]
libnanami = { path = "../../sdk/rust/libnanami" }
nanami-services = { path = "../../sdk/rust/nanami-services" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace]
