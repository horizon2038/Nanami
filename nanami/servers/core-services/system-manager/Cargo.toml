[package]
name = "system-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
a9n_abi = "0.6.0"
libnanami = { path = "../../sdk/rust/libnanami" }
nanami-services = { path = "../../sdk/rust/nanami-services" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace]
