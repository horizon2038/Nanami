[package]
name = "timer_server"
version = "0.1.0"
edition = "2021"

[dependencies]
libnanami = { path = "../../sdk/rust/libnanami" }
nanami-services = { path = "../../sdk/rust/nanami-services" }

[features]
default = []
hpet = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace]
